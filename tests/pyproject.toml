[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "clean-arch-app"
version = "0.1.0"
description = "Clean architecture demo"
requires-python = ">=3.9"

[tool.python.ini_options]
pythonpath = ["."]
